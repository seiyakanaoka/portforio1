<div class="container all">
  <div class="row">
    </div class="col-xs-10 col-sm-10 col-md-10 col-lg-10">


    <% unless params[:name] == nil %>
      <div class="row mb-5">
        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
          <% @log.each do |log| %>
            <%= link_to log_path(log.id) do %><%= attachment_image_tag log, :log_image, size: "200x200" %><% end %>
          <% end %>
        </div>
      </div>

      <div class="row">
        <div class="col-xs-8 col-sm-8 col-md-8 col-lg-8 offset-xs-2 offset-sm-2 offset-md-2 offset-lg-2 hashtag-name">
          <% @hashtags.sort.reverse.each do |count| %>
            <% count[1].each do |hashtag| %>
              <%= link_to  "##{hashtag.hashname} (#{hashtag.logs.count}) 件","/log/hashtag/#{hashtag.hashname}",data: {"turbolinks" => false} %>
            <% end %>
          <% end %>
        </div>
      </div>
    <% end %>

    </div>
  </div>
</div>