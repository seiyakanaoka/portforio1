<div class="container all">
  <div class="row">
    <div class="col-xs-10 col-sm-10 col-md-10 col-lg-10 offset-xs-1 offset-sm-1 offset-md-1 offset-lg-1">
      <div class="row">
        <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">

          <h1 class="text-center mb-5"><%= @user.name %>さん</h1>

          <table class="table table-borderless">
            <thead>
              <th class="h1 text-center"><strong><i class="fas fa-exclamation-triangle text-warning"></i>退会確認<i class="fas fa-exclamation-triangle text-warning"></i></strong></th>
            </thead>
            <tbody>
              <tr>
                <td class="text-center">
                  一度退会すると今後このアカウントは使用できなくなります<br />
                  本当に退会してもよろしいですか？
                </td>
              </tr>
              <tr>
                <td></td>
              </tr>
              <tr></tr>
            </tbody>
          </table>

        </div>
      </div>

      <div class="row">
        <div class="col-xs-4 col-sm-4 col-md-4 col-lg-4 offset-xs-1 offset-sm-1 offset-md-1 offset-lg-1 edit-btn">
          <%= link_to my_page_user_path(current_user.id), class: "btn edit-btn-gradation rounded-pill" do %>退会しない<% end %>
        </div>

        <div class="col-xs-4 col-sm-4 col-md-4 col-lg-4 offset-xs-2 offset-sm-2 offset-md-2 offset-lg-2 danger-btn">
          <%= link_to withdraw_user_path, method: :patch, data: { confilm: "本当に退会しますか？" }, class: "btn danger-btn-gradation rounded-pill" do %>退会する<% end %>
        </div>
      </div>

    </div>
  </div>
</div>